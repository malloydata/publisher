// No dashboards/orphan.malloy sits beside this file, so it is a component that
// can never render — the lint says so at load rather than leaving it silent.
export default function Orphan() {
   return null;
}
