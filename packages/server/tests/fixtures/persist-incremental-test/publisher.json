{
   "name": "persist-incremental-test",
   "version": "1.0.0",
   "description": "DuckDB persist fixture declaring refresh=\"incremental\" with a watermark. The declaration is coherent in itself, but DuckDB is off the incremental dialect allowlist, so the gate rejects it — and a rejection is fatal wherever the package is admitted: a publish answers 400 and the package does not load."
}
