{
  "name": "jaffle-shop-convert",
  "version": "1.0.0",
  "description": "Convert: the same semantic layer, over marts Malloy rebuilt from the raw data and materialized with #@ persist. Same interface, different plumbing."
}
